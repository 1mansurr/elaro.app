project_id = "alqpwhrsxmetwbtxuihv"

# Database connection settings
# Use direct connection (port 5432) for migrations instead of pooler (port 6543)
# This is recommended for migrations and admin tasks
[db]
# Direct connection is used by default for migrations
# Pooler connection (port 6543) is used for application queries

[functions.revenuecat-webhook]
verify_jwt = false

[functions.export-user-data]
verify_jwt = false

[functions.check-grace-period]
verify_jwt = false

[functions.send-welcome-email]
verify_jwt = false
